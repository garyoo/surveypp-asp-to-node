export enum LogicMode {
    ASP = 1,
    JAVASCRIPT = 2,
}

export enum Lang {
    KOR = 1,
    ENG = 2,
}

export enum GlobalReplacer {
    MBOX = '//mbox:',
}

export enum GroupID {
    TG = 'TG',
}