<%
if (query.pid === undefined || query.pid === '') return 'invalid parameters!!!';
%>
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>LOGIN</title>
</head>
<body class="bg-secondary">
<div class="container-fluid mb-4">
    <form method="post" name="login-form" id="login-form" action="/api/getAuth">
        <input type="hidden" name="projectID" value="<%=query.pid%>"/>
        <%if(res.locals.redirectUrl){%><input type="hidden" name="redirectUrl" value="<%=res.locals.redirectUrl%>"/><%}%>
        <div class="row">
            <div class="col-12 col-sm-6 col-lg-4 offset-sm-3 offset-lg-4 mt-4 shadow rounded bg-white" id="login">
                <div class="form-group my-4 text-center">
                    인증키를 입력해주세요.
                </div>
                <div class="form-group mt-4">
                    <input type="password" class="form-control" placeholder="인증키" name="authKey" id="auth-key" required/>
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-danger btn-block">인증</button>
                </div>
            </div>
        </div>
    </form>
</div>
</body>
</html>